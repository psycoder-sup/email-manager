// Production Configuration
// This file contains production-specific settings

// Google OAuth Client ID for production (iOS OAuth client type)
// Set via environment variable: export GOOGLE_CLIENT_ID="your-id.apps.googleusercontent.com"
// For CI/CD, use secrets management
// Note: iOS OAuth clients don't require a client secret (PKCE is used instead)
GOOGLE_CLIENT_ID = $(GOOGLE_CLIENT_ID)

// Environment identifier
APP_ENVIRONMENT = production

// Production optimizations
SWIFT_OPTIMIZATION_LEVEL = -O
