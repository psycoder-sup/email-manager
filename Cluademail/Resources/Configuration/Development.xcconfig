// Development Configuration
// This file contains development-specific settings

// Google OAuth Client ID for development
// Set via environment variable: export GOOGLE_CLIENT_ID="your-id.apps.googleusercontent.com"
GOOGLE_CLIENT_ID = $(GOOGLE_CLIENT_ID)

// Google OAuth Client Secret for development
// Set via environment variable: export GOOGLE_CLIENT_SECRET="your-secret"
GOOGLE_CLIENT_SECRET = $(GOOGLE_CLIENT_SECRET)

// Environment identifier
APP_ENVIRONMENT = development

// Bundle ID suffix for development builds (optional)
// PRODUCT_BUNDLE_IDENTIFIER = com.cluademail.app.dev

// Enable additional debugging
OTHER_SWIFT_FLAGS = $(inherited) -D DEBUG
